# 🌹 Atlas Rose Bot

Telegram бот для магазина букетов из атласных роз ручной работы.

## Установка и запуск

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка
Создайте файл `.env`:
```
BOT_TOKEN=ваш_токен_от_BotFather
```

### 3. Запуск локально
```bash
python bot.py
```

### 4. Деплой на Railway

1. Создай репозиторий на GitHub
2. Добавь файлы:
```bash
git init
git add .
git commit -m "Initial commit"
git push
```

3. В Railway:
   - Подключи репозиторий
   - Добавь переменную: `BOT_TOKEN=твой_токен`
   - Railway автоматически задеплоит

## Структура

```
flower_bot/
├── bot.py              # Главный файл
├── config.py           # Конфигурация
├── handlers/           # Обработчики команд
│   ├── client.py       # Клиентские команды
│   └── admin.py        # Админские команды
├── keyboards/          # Клавиатуры
├── database/          # Работа с YAML
└── data/              # YAML файлы
```

## Функционал

### Для клиентов:
- 🌹 Каталог букетов с фото
- 🛒 Конструктор заказа (цвет, количество, упаковка)
- ⚡️ Дополнительные услуги (срочный заказ, открытка)
- 📅 Выбор даты и времени
- 📍 Способ получения
- 🛒 Корзина
- 💳 Оплата
- ⭐️ Избранное
- 📦 История заказов

### Для админа:
- 📊 Статистика
- 📦 Просмотр заказов
- ➕ Добавление букетов
- ✏️ Редактирование каталога

## Команды

- `/start` - Запуск бота
- `/admin` - Админ-панель (только для админа)

## Автор

@thesun4ck
